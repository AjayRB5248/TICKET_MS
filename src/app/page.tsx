import { HomeView } from "src/sections/home/view";

export const metadata = {
  title: "Hulya Events: Explore The Best Events in Australia",
};

export default function HomePage() {
  return <HomeView />;
}
